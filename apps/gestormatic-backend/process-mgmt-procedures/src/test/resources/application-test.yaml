spring:
  config:
    import: ""
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: procedures

app:
  supabase:
    url: http://localhost:54321
    jwks: ""
